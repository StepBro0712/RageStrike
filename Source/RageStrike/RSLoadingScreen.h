#pragma once

#include "CoreMinimal.h"

// Экран загрузки: чёрный фон, проявляющаяся надпись RAGESTRIKE и крутящийся
// индикатор. Показывается, пока грузится уровень — раньше на этом месте
// было просто чёрное окно без единого признака жизни.
namespace RSLoadingScreen
{
	// подписывается на события смены карты; зовётся один раз при старте модуля
	void Register();
	void Unregister();
}
